ul.inactivepms
	if items.length > 0 
		each user in items
			li.item(data-id=user.id, key=user.id)
				a(href="/chat/@" + user.username)
					.avatar-wrap
						if user.status === 16
							span.online
						else
							span.offline
						img(src="#{user.avatar}",width="20",alt="#{user.username}")
						.avatar-status(class=(user.status === 16 ? 'online' : 'offline'))
					span.name=user.username
