.roommemberspopover
	h4=_('Members in ') + room.name
	span.close ×
	ul
		each user in room.users
			li.item.user
				.avatar-wrap
					span.online
					img(src=user.avatar, width="20", alt=user.username)
					.avatar-status.online
				span.name= user.username
