h1.roomname= room.name
span.usersonline
	em #{room.users.length} users in this room: 
	each user in room.users
		span.user(class="colors-" + user.num) #{user.username} (#{user.status}) 
form.searchform
	input.search(type="search", placeholder=_("search for content, documents, appointments, …"))
	input.search-submit(type="submit")

