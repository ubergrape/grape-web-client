{
  "name": "grape-theme",
  "private": true,
  "description": "Theme related files sharable between all ubergrape projects.",
  "version": "0.1.1",
  "engines": {
    "node": ">=0.8.0"
  },
  "devDependencies": {
    "babel-core": "^5.6.20"
  },
  "scripts": {
    "build": "./bin/build"
  }
}
