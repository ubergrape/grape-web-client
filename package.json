{
  "name": "grape-theme",
  "description": "Theme related files sharable between all ubergrape projects.",
  "version": "3.5.1",
  "engines": {
    "node": ">=0.8.0"
  },
  "devDependencies": {
    "babel-core": "^5.6.20",
    "color": "^0.10.1",
    "eslint-config-ubergrape": "^2.1.0"
  },
  "scripts": {
    "all": "yarn run clean && yarn run build",
    "build": "./bin/build",
    "clean": "rm -fr ./dist/*",
    "prepublishOnly": "yarn run all"
  },
  "files": [
    "dist"
  ]
}
