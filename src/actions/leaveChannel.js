import * as types from '../constants/actionTypes'

export const showLeaveChannelDialog = () => ({
  type: types.SHOW_LEAVE_CHANNEL_DIALOG,
})

export const hideLeaveChannelDialog = () => ({
  type: types.HIDE_LEAVE_CHANNEL_DIALOG,
})
