import theme from '../../../../constants/theme'

export default {
  item: {
    isolate: false,
    border: 0,
    height: 24,
    background: theme.colorBackgroundLight,
    display: 'flex',
    alignItems: 'center',
    borderRadius: 12,
    padding: '0px 8px',
    margin: '2px 4px',
    color: theme.colorTextBase,
    cursor: 'pointer',
  },
}
