const size = 32

export const styles = {
  avatar: {
    display: 'block',
    position: 'relative',
    width: size,
    height: size,
    flexShrink: 0,
    borderRadius: '50%',
    background: 'no-repeat center',
    backgroundSize: '100%'
  }
}
