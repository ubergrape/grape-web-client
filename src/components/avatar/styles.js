const size = 32

export default {
  avatar: {
    display: 'block',
    width: size,
    height: size,
    flexShrink: 0,
    borderRadius: '50%',
    background: 'no-repeat center',
    backgroundSize: '100%'
  }
}
