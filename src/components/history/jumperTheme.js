import buttonIcon from '../button/icon'

export const styles = {
  jumper: {
    extend: buttonIcon('arrowDown'),
    position: 'absolute',
    fontSize: 50,
    bottom: 20,
    left: '50%',
    marginLeft: -25,
    background: 'none',
    cursor: 'pointer'
  }
}
