import {white} from 'grape-theme/dist/base-colors'

export const styles = {
  history: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  },
  separatorDate: {
    background: white
  }
}
