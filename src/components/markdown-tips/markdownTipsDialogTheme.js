import {borderDefault} from 'grape-theme/dist/web-colors'

export const styles = {
  wrapper: {
    padding: 20,
    borderTop: {
      width: 3,
      style: 'solid',
      color: borderDefault
    },
    maxHeight: '80vh',
    overflowY: 'auto'
  }
}
