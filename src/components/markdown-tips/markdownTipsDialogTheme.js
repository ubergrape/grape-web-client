import {borderDefault} from 'grape-theme/dist/web-colors'

export const styles = {
  wrapper: {
    padding: 20,
    borderTop: `3px solid ${borderDefault}`,
    maxHeight: '80vh',
    overflowY: 'auto'
  }
}
